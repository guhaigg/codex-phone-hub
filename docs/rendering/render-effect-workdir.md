# Render Effect Report

位置：`/Users/example/Documents/work/codex-phone-hub/docs/rendering/render-effect-workdir.md`

## 目的

这份报告放在工作目录下，用来验证：

- 是否还会被误识别为 app 内 report
- Markdown 基础元素是否正常渲染
- 严格模式下它是否应当只被当作普通本地文档

## 列表

- item one
- item two
- item three

## 编号

1. alpha
2. beta
3. gamma

## 强调

这里有 **bold**、*italic*、`inline code`。

## 代码块

```js
const source = 'workspace-root';
console.log(source);
```

## 引用

> This file is under the repository working directory.

## 路径

当前项目：`codex-phone-hub`
